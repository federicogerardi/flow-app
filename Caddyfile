:{$PORT}

handle /api/* {
    reverse_proxy backend.railway.internal:3000
}

handle /health {
    respond "ok" 200
}

handle {
    root * /usr/share/caddy
    encode gzip
    file_server
    try_files {path} /index.html
}