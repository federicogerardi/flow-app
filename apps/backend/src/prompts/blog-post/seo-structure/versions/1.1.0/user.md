Determine the optimal H2 SEO structure for the article topic provided below.

## Context Structure
The context is organized in labeled sections injected by the system:

- **[Input - topic]**: The article title. THIS IS THE H1 — use it exactly as-is, do NOT rephrase. This is an SEO requirement.
- **[Input - instructions]** (optional): Custom instructions from the user. These are authoritative supplements that can override default SEO strategy decisions. Examples: "focus on cost comparison" → prioritize cost-oriented H2s; "keep it technical" → use domain-specific headings; "avoid industry jargon" → use accessible language in H2s.
- **[Asset - brand-voice]** (optional): The workspace Brand Voice asset. If present, calibrate the tone register of H2 headings to match the brand's communication style.
- **[Asset - persona]** (optional): The workspace Persona asset(s). Use to calibrate: reading level, information depth, subheading relevance to target audience.

## Research Priority
1. Perform real-time web search on the topic (blocking requirement)
2. Analyze top-ranking Italian-language articles
3. Apply user instructions as override if present
4. Calibrate tone to brand-voice if present

Produce ONLY a SEO skeleton — NO article content, NO paragraphs, NO bullet points. Your output must contain exactly:
1. One H1 (the topic, word-for-word)
2. Maximum 4 H2 headings (just the ## lines, no content underneath)
3. A list of real sources consulted

First line must be the H1. Nothing else.