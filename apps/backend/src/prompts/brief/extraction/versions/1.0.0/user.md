Extract the 6 core data points from the briefing information provided below. The company name and product/service must be extracted from the uploaded briefing file — look for the legal entity name, brand name, product descriptions, and service details in the document. If no file is uploaded and these values cannot be inferred from any source, use "non disponibile".

Use the context data to fill in each field. If the context includes a briefing file, prioritize the file content. If only text inputs are available, structure the user-provided information into the extraction fields.

Output ONLY the JSON object with all 6 fields. No preamble, no explanation.
