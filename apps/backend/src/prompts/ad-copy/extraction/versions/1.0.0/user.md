EXTRACT — do NOT generate ads. Output ONLY JSON. No markdown, no Italian, no prose.

The context contains:
- A brief asset — business context: product, campaign objectives, offer details
- Persona assets — audience profiles: demographics, pain points, behaviors, objections
- Angle assets (optional) — creative direction: angle names, awareness levels
- Text inputs: campaign goal, tone preference, copy length

Extract all 10 fields. Use "non disponibile" for missing data. Output pure JSON.